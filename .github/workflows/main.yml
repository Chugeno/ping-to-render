name: keep-alive

on:
  schedule:
    - cron: "*/14 * * * *"    # CAMBIO CLAVE: Cada 14 minutos
  workflow_dispatch:          # Te permite probarlo manualmente con un botón

jobs:
  ping:
    runs-on: ubuntu-latest
    timeout-minutes: 5        # Seguridad por si se cuelga
    steps:
      - name: Wake Render (GET)
        run: curl -fsS https://n8n-dnlu.onrender.com/healthz
